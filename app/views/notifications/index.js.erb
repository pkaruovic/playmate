<% if @notifications.first_page? %>
    $("#notification-list").html("<%= j(render "notifications", notifications: @notifications) %>")
<% else %>
    $("#notification-list").append("<%= j(render "notifications", notifications: @notifications) %>");
<% end %>
