<% if @posts.empty? %>
  <%= render partial: "empty_state" %>
<% else %>
  <div class="posts-list">
    <h1>Posts</h1>
    <%= render partial: "posts/post", collection: @posts %>
    <%= paginate @posts %>
  </div>
<% end %>
